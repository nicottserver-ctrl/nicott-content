# 인계 체크리스트

> 이 문서 하나로 인계가 끝나야 합니다. 항목을 위에서부터 체크하며 진행하세요.
> 원칙: **소유권은 처음부터 대표님, 도와주는 사람은 초대받아 들어간다.** 나중에 옮기는 방식은 쓰지 않습니다.

---

## A. 소유권 — 전부 대표님 명의여야 하는 것

| 항목 | 만드는 사람 | 확인 방법 | 체크 |
|---|---|---|---|
| GitHub 계정 | 대표님 (구글 계정으로 가입) | Settings에 대표님 이메일 | ☐ |
| 저장소 `nicott-content` | 대표님 | Settings → 소유자 = 대표님 계정 | ☐ |
| Meta 개발자 계정·앱 | 대표님 | 앱 대시보드 역할에 대표님이 관리자 | ☐ |
| Cloudflare 계정 | 대표님 | 계정 이메일 | ☐ |
| 인스타 비즈니스 계정 | 이미 보유 | @nicott_kitchen 관리 권한 | ☐ |
| Anthropic API 키 (선택) | 대표님 | 결제 수단이 대표님 것인지 | ☐ |

> ⚠️ 도와주는 사람 계정으로 먼저 만들고 나중에 옮기면 **주소가 바뀌고 데이터가 유실됩니다.** 반드시 위 순서로.

---

## B. 시크릿 — 저장소 Settings → Secrets and variables → Actions

| 이름 | 값 | 필수 | 체크 |
|---|---|---|---|
| `IG_TOKEN` | 인스타 장기 액세스 토큰 (60일) | ✅ | ☐ |
| `IG_USER_ID` | 인스타 계정 ID (대시보드 연결 테스트하면 표시) | ✅ | ☐ |
| `ANTHROPIC_API_KEY` | 템플릿 자동 생성용 | 선택 | ☐ |

> 토큰은 **저장소 파일에 절대 넣지 않습니다.** 대시보드의 `저장소에 동기화`도 토큰을 제외하고 올립니다.

---

## C. 접근 권한 — 도와주는 사람

| 항목 | 방법 | 인계 시 조치 |
|---|---|---|
| 저장소 | Settings → Collaborators → 초대 | 필요 없어지면 **Remove** |
| Cloudflare | Members → 초대 | Remove |
| Meta 앱 | 앱 역할 → 개발자 추가 | 역할 삭제 |
| 대시보드 | Cloudflare Access 허용 이메일 | 목록에서 제거 |

**이것만 빼면 완전히 대표님 것이 됩니다.** 코드·데이터·기록은 전부 저장소 안에 있으므로 따로 넘길 파일이 없습니다.

---

## D. 저장소에 들어가야 할 것

```
nicott-content/
├─ index.html                        대시보드 (니코트_대시보드_v1.1.html)
├─ .github/workflows/
│   ├─ publish.yml                   예약 발행 (30분마다)
│   ├─ trend-monthly.yml             벤치마크 수집 (매월 12일)
│   └─ trend-ai-template.yml         템플릿 자동 생성 (매월 13일)
├─ scripts/validate-templates.js     자동 생성 검증기
├─ config/
│   ├─ benchmark.json                관찰 계정 목록
│   ├─ brand-tokens.json             브랜드 규칙 (AI가 지켜야 할 것)
│   ├─ template-example.js           템플릿 작성 형식
│   └─ known-ids.json                아이디 중복 방지 (자동 갱신)
├─ data/                             자동 생성 — 손대지 않아도 됨
└─ 문서/
    ├─ README_설치순서.md
    ├─ 인계_체크리스트.md            ← 이 문서
    ├─ 대표님_운영매뉴얼.md
    ├─ handoff.md                    그동안의 결정 기록
    └─ mistake.md                    반복하면 안 되는 실수 기록
```

> `handoff.md`·`mistake.md`를 함께 넣는 이유: **나중에 누가 이어받아도 맥락이 그대로 넘어갑니다.**

---

## E. 인계 전 반드시 확인 (동작 검증)

아래를 실제로 한 번씩 해보고 체크하세요. 하나라도 안 되면 인계하지 않습니다.

| 확인 | 방법 | 통과 기준 | 체크 |
|---|---|---|---|
| 대시보드 열림 | Cloudflare 주소를 폰·PC에서 | 둘 다 정상 표시 | ☐ |
| 저장 유지 | 콘텐츠 하나 만들고 브라우저 껐다 켜기 | 그대로 남아 있음 | ☐ |
| 인스타 연결 | 설정 → 연결 테스트 | 계정명·ID 표시 | ☐ |
| 저장소 연결 | 설정 → 연결 테스트 | 저장소 이름 표시 | ☐ |
| **실제 게시** | 테스트 게시물 1건 발행 | 인스타에 실제로 올라감 | ☐ |
| 예약 자동 발행 | 10분 뒤로 예약 → 동기화 → 대기 | 컴퓨터 꺼도 올라감 | ☐ |
| 템플릿 자동 생성 | Actions → trend-ai-template 수동 실행 | 갤러리에 '새로 도착한 템플릿' 표시 | ☐ |
| 백업 | 설정 → 백업 파일 내려받기 | JSON 파일 저장됨 | ☐ |

---

## F. 인계 자리에서 함께 해볼 것 (30분)

1. 대표님이 **직접** 사진 한 장으로 게시물 1건 만들기 → 검수 → 예약
2. 캘린더에서 반복 일정 한 번 걸어보기
3. 갤러리에서 템플릿 ★ 저장 / 보관 눌러보기
4. 백업 파일 받아서 안전한 곳에 보관
5. 토큰 만료일(발급일 +60일)을 대표님 달력에 등록

---

## G. 인계 후 대표님이 혼자 해야 하는 일

| 주기 | 할 일 | 소요 |
|---|---|---|
| 매일 | 만들고 예약 | 10~20분 |
| 월 1회 | 갤러리에서 새 템플릿 확인 → 쓸 것만 [쓸게요] | 5분 |
| **60일마다** | **인스타 토큰 재발급** → 대시보드·Secrets 교체 | 10분 |
| 분기 1회 | 백업 파일 받아두기 | 1분 |

> **60일 토큰 재발급이 유일한 정기 작업입니다.** 잊으면 게시가 실패하고 대시보드에 오류가 표시됩니다. 달력 알림을 꼭 걸어두세요.
