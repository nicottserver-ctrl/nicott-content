# 니코트 콘텐츠 시스템 — 설치·인계 순서

> 이 폴더의 내용을 **대표님 명의 GitHub 저장소**에 올리면 시스템이 서버에서 자동으로 돌기 시작합니다.
> 로컬 HTML 파일만으로는 브라우저를 열어둔 동안에만 동작합니다. 예약 발행·월간 트렌드 리서치는 저장소에 올라가야 자동화됩니다.

---

## 왜 대표님 명의여야 하나

계정을 사장님 명의로 만들어두고 나중에 옮기면 반드시 문제가 생깁니다 — 이전이 안 되는 서비스가 있고, 주소가 바뀌며, 그 과정에서 데이터가 유실됩니다.
**처음부터 대표님 명의로 만들고, 사장님을 협업자로 초대**하는 순서가 맞습니다. 인계일에는 협업자만 빼면 그대로 대표님 것이 됩니다.

구글 계정으로 GitHub·Cloudflare 모두 가입할 수 있습니다.

---

## 1단계 · GitHub 저장소 (15분)

1. github.com 가입 — **대표님 구글 계정**으로 로그인
2. **New repository** → 이름 `nicott-content` → **Public** → Create
   - 공개로 두는 이유: 인스타가 이미지를 가져가려면 이미지 주소가 공개여야 합니다. 토큰·비밀번호는 저장소에 올리지 않습니다.
3. 이 폴더의 파일을 저장소에 올립니다 (드래그해서 업로드해도 됩니다)

```
nicott-content/
├─ index.html                     ← 대시보드 (니코트_대시보드_v1.1.html 을 이 이름으로)
├─ .github/workflows/
│   ├─ publish.yml                ← 예약 발행 (30분마다)
│   └─ trend-monthly.yml          ← 월간 트렌드 리서치 (매월 12일)
├─ scripts/validate-templates.js  ← 자동 생성 검증기
├─ config/
│   ├─ benchmark.json             ← 관찰할 벤치마크 계정 목록
│   ├─ brand-tokens.json          ← AI가 지켜야 할 브랜드 규칙
│   ├─ template-example.js        ← 템플릿 작성 형식
│   └─ known-ids.json             ← 아이디 중복 방지 (자동 갱신)
└─ data/                          ← 자동으로 채워집니다 (손대지 않아도 됨)
```

> 문서도 함께 올리세요 — `인계_체크리스트.md` · `대표님_운영매뉴얼.md` · `handoff.md` · `mistake.md`.
> 저장소 하나에 코드·데이터·기록이 모두 있으면 **누가 이어받아도 맥락이 그대로 넘어갑니다.**

4. Settings → Secrets and variables → Actions → **New repository secret** 2개 등록
   - `IG_TOKEN` — 인스타 장기 액세스 토큰
   - `IG_USER_ID` — 인스타 계정 ID (대시보드 설정에서 연결 테스트하면 표시됩니다)

---

## 2단계 · 인스타 토큰 (10분)

**앱 심사는 필요 없습니다.** 본인 계정만 사용하는 앱은 Meta의 Standard Access로 충분합니다.

1. developers.facebook.com → 개발자 등록 → **앱 만들기** (유형: 비즈니스)
2. 제품에서 **Instagram** 추가 → **API setup with Instagram login**
3. **Add account** 로 @nicott_kitchen 연결
4. 같은 화면의 **Generate token** → 60일 장기 토큰 복사
5. 대시보드 설정 탭에 붙여넣고 **연결 테스트** → 계정 ID가 자동으로 채워집니다
6. 그 토큰과 ID를 위 1단계의 Secrets에도 등록

> 토큰은 60일마다 재발급이 필요합니다. 만료되면 게시가 실패하고 대시보드에 표시됩니다.

---

## 3단계 · Cloudflare Pages (10분)

대시보드를 주소로 띄웁니다. 대표님이 링크만 열면 되고, 휴대폰에서도 그대로 열립니다.

1. dash.cloudflare.com 가입 — 대표님 구글 계정
2. **Workers & Pages** → **Create** → **Pages** → **Connect to Git**
3. 방금 만든 `nicott-content` 저장소 선택
4. 빌드 설정: 프레임워크 없음, 빌드 명령 비움, 출력 디렉터리 `/` → **Save and Deploy**
5. 1~2분 뒤 `nicott-content.pages.dev` 같은 주소가 나옵니다
6. (권장) Settings → **Cloudflare Access** 로 접근 제한 — 대표님 이메일만 열 수 있게

이후 저장소가 바뀔 때마다 자동으로 재배포됩니다.

---

## 4단계 · 첫 동기화

1. 대시보드(주소 또는 로컬 파일)를 열고 설정 탭에서 저장소 연결
2. **저장소에 동기화** 클릭 → `data/db.json` 이 생깁니다
3. 이 시점부터 예약 발행이 서버에서 자동으로 돕니다 (컴퓨터를 꺼도 됩니다)

---

## 무엇이 자동이고 무엇이 사람 몫인가

| | 자동 | 사람 |
|---|---|---|
| 예약 발행 | ✅ 30분마다 확인 후 게시, 게시물 ID 회수·기록 | 승인·검수 |
| 월간 트렌드 수집 | ✅ 매월 12일 벤치마크 지표 수집 + 이슈 알림 | — |
| **새 템플릿 디자인** | ✅ 매월 13일 AI가 후보 3종 생성 → PR | **Merge 여부 판단 (미리보기 보고 클릭 한 번)** |
| 이미지 생성 | ✅ 사진 넣으면 템플릿 합성 | 사진 촬영·선택 |
| 캡션 | ✅ 규칙 조립 초안 | 최종 문장 다듬기 |

---

## 5단계 · 템플릿 자동 생성 (반년 뒤를 위한 준비)

Secret `ANTHROPIC_API_KEY` 를 등록하면 **매월 13일 시스템이 새 템플릿을 만들어 대시보드로 보냅니다.**
대표님은 갤러리 맨 위 **새로 도착한 템플릿**에서 미리보기를 보고 **[쓸게요]** 만 누르면 됩니다. GitHub을 열 필요가 없습니다.

**전체 흐름 — 사람 손이 닿는 지점은 한 곳뿐**

```
매월 12일  벤치마크 12계정 지표 수집 (자동)
매월 13일  AI가 그달 문법을 읽고 템플릿 3종 생성 (자동)
           ↓
           규칙 검증기가 위반 항목 자동 반려 (자동)
           ↓
           data/templates/pending.json 로 전달 (자동)
           ↓
대시보드   "새로 도착한 템플릿" 에 미리보기 표시 (자동)
           ↓
대표님     [쓸게요] 또는 [보관]  ←── 사람이 하는 유일한 판단
           ↓
           갤러리에 편입 → 세트에서 바로 사용
```

**AI가 마음대로 못 하게 막아둔 것 (검증기 11종)**

| 검사 | 내용 |
|---|---|
| 색 | `#hex`·`rgb()` 직접 사용 금지 — 브랜드 토큰 변수만 |
| 변수 | 허용 목록에 없는 CSS 변수 사용 차단 |
| 범위 | CSS 선택자가 자기 아이디로 시작하지 않으면 반려 |
| 캔버스 | 1080×1350 / 1080×1920 외 반려 |
| 안전영역 | 세로형에서 하단 250px에 요소가 있으면 반려 |
| 슬롯 | 자리표시자와 입력칸이 안 맞으면 반려 |
| 보안 | `<script>`·`on*=`·`javascript:` 포함 시 반려 |
| 표현 | 금지 표현(역대급·최저가 등) 포함 시 반려 |
| 중복 | 이미 있는 아이디면 반려 |
| 형식 | 필수 항목 누락·잘못된 열거값 반려 |
| 실행 | 템플릿은 **코드가 아니라 문자열** — 대시보드가 실행하지 않고 치환만 합니다 |

> 실제 테스트: 일부러 위반 8종을 넣어 돌렸더니 **7종을 정확히 반려하고 정상 1종만 통과**시켰습니다. 반려 사유는 `data/templates/_check.md` 에 기록됩니다.

**비용** — 월 1회 실행 기준 수백 원. 키를 넣지 않으면 이 워크플로만 조용히 건너뛰고 나머지는 그대로 돕니다.

---

## 인계 후 원격 지원은 어떻게 되나

소유권 이전과 접근 권한은 별개입니다.

- **저장소**: 대표님이 소유자, 도와주는 사람은 **협업자(Collaborator)** 로 초대 — 언제든 추가·제거 가능
- **대시보드**: Cloudflare 주소라 어디서든 열립니다. Access 허용 목록에 이메일만 추가하면 됨
- **맥락**: `handoff.md` · `mistake.md` 가 저장소에 함께 있어, 누가 이어받아도 그동안의 결정과 실수 기록이 그대로 넘어갑니다

즉 **인계했다고 원격 관리가 끊기지 않습니다.** 필요 없어지는 시점에 협업자에서 빠지면 그때 완전히 대표님 것이 됩니다.

---

## 비용

| 항목 | 비용 |
|---|---|
| GitHub (저장소·Actions) | 0원 (공개 저장소는 Actions 무제한) |
| Cloudflare Pages | 0원 |
| Meta Graph API | 0원 |
| **합계 고정비** | **월 0원** |

---

## 문제가 생기면

| 증상 | 원인 | 조치 |
|---|---|---|
| 게시 실패 · 토큰 오류 | 60일 만료 | 2단계 반복해 토큰 재발급 → 대시보드·Secrets 둘 다 교체 |
| 이미지가 안 올라감 | 저장소가 비공개 | Settings → General → Change visibility → Public |
| 예약이 안 나감 | 동기화 안 함 | 대시보드 설정 → 저장소에 동기화 |
| Actions가 안 돎 | 워크플로 비활성 | 저장소 Actions 탭 → Enable workflows |
